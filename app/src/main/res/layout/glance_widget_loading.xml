<?xml version="1.0" encoding="utf-8"?>
<!--
    Required by AppWidgetProviderInfo.initialLayout. This is what the widget
    host briefly shows before Glance composes the real UI — you will not
    actually see this once the widget is running, so keep it trivial.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFFFF" />